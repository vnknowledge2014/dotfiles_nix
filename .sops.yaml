# SOPS configuration file
# For more information, see: https://github.com/mozilla/sops

creation_rules:
  - path_regex: secrets/.*\.yaml$
    key_groups:
      - age:
          - "age1..." # Replace with your age public key (generated via: age-keygen -o key.txt)
